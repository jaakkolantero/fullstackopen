https://puhelinluettelo.ufo-x.now.sh/

Add secret to now:

```
now secret add -- mongodb-uri verisekretpassw0rd
```

The -- is Posix standard way to indicate there won't be any more flags for the command.

deploy

```
now
```
